# Local environment files — `.env.example` is the committed template.
.env
.env.*
!.env.example

# Editor / OS noise
.idea/
.vscode/
.DS_Store
Thumbs.db

# Claude Code personal settings (shared ones live in settings.json)
.claude/settings.local.json
